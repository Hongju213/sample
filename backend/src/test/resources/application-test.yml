spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:sample;MODE=Oracle;DATABASE_TO_UPPER=true;DB_CLOSE_DELAY=-1
    username: sa
    password:
  sql:
    init:
      mode: always

app:
  cors:
    allowed-origins: http://localhost:5173
