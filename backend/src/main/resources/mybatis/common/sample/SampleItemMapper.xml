<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.sample.sample.mapper.SampleItemMapper">

    <resultMap id="SampleItemDtoResultMap" type="com.example.sample.sample.dto.SampleItemDto">
        <id  property="id"          column="ID"/>
        <result property="title"       column="TITLE"/>
        <result property="description" column="DESCRIPTION"/>
        <result property="status"      column="STATUS"/>
        <result property="createdAt"   column="CREATED_AT"/>
        <result property="updatedAt"   column="UPDATED_AT"/>
    </resultMap>

    <sql id="searchWhere">
        <where>
            <if test="dto.keyword != null and dto.keyword != ''">
                AND (LOWER(TITLE) LIKE '%' || LOWER(#{dto.keyword}) || '%'
                  OR LOWER(DESCRIPTION) LIKE '%' || LOWER(#{dto.keyword}) || '%')
            </if>
            <if test="dto.status != null">
                AND STATUS = #{dto.status}
            </if>
        </where>
    </sql>

    <select id="findAllBySampleItemDto" resultMap="SampleItemDtoResultMap">
        SELECT ID, TITLE, DESCRIPTION, STATUS, CREATED_AT, UPDATED_AT
        FROM SAMPLE_ITEM
        <include refid="searchWhere"/>
        ORDER BY ID DESC
        OFFSET #{offset} ROWS FETCH NEXT #{limit} ROWS ONLY
    </select>

    <select id="countBySampleItemDto" resultType="int">
        SELECT COUNT(*)
        FROM SAMPLE_ITEM
        <include refid="searchWhere"/>
    </select>

    <select id="findById" resultMap="SampleItemDtoResultMap">
        SELECT ID, TITLE, DESCRIPTION, STATUS, CREATED_AT, UPDATED_AT
        FROM SAMPLE_ITEM
        WHERE ID = #{id}
    </select>

    <insert id="insert" parameterType="com.example.sample.sample.dto.SampleItemDto">
        <selectKey keyProperty="id" resultType="long" order="BEFORE">
            SELECT SAMPLE_ITEM_SEQ.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO SAMPLE_ITEM (ID, TITLE, DESCRIPTION, STATUS, CREATED_AT, UPDATED_AT)
        VALUES (#{id}, #{title}, #{description}, #{status}, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP)
    </insert>

    <update id="update" parameterType="com.example.sample.sample.dto.SampleItemDto">
        UPDATE SAMPLE_ITEM
        SET TITLE       = #{title},
            DESCRIPTION = #{description},
            STATUS      = #{status},
            UPDATED_AT  = CURRENT_TIMESTAMP
        WHERE ID = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM SAMPLE_ITEM WHERE ID = #{id}
    </delete>

</mapper>
